---
trigger: always_on
---
Los componentes deben ser funcionales (no clases).

Props tipadas con interface o type.

Evita lógica compleja en la UI → muévela a hooks o servicios.

Un componente = una responsabilidad.
